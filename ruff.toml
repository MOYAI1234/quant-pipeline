target-version = "py310"
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]

[lint.per-file-ignores]
"**/__init__.py" = ["F401"]
# 聚宽在运行时注入 g、log、set_benchmark 等全局对象。
"examples/joinquant/*.py" = ["F821"]
